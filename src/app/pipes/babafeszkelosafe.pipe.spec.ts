import { BabafeszkelosafePipe } from './babafeszkelosafe.pipe';

describe('BabafeszkelosafePipe', () => {
  it('create an instance', () => {
    const pipe = new BabafeszkelosafePipe();
    expect(pipe).toBeTruthy();
  });
});
