import { LombikrasafePipe } from './lombikrasafe.pipe';

describe('LombikrasafePipe', () => {
  it('create an instance', () => {
    const pipe = new LombikrasafePipe();
    expect(pipe).toBeTruthy();
  });
});
